@model Equipe
@{
    ViewData["Title"] = "Equipes";
}


<section class="cadastro_times">
    <div class="escudo">
        <div class="flex_container">
            <div class="titulo_banner">
                <h1>Cadastro de Times</h1>
                <img src="~/_assets/decoracao_cadastro.svg" alt="Decoração cadastro">
            </div>

            <form method="POST" action='@Url.Action("Cadastrar")' enctype="multipart/form-data">
                <input class="input_text" type="text" placeholder="Id" name="IdEquipe">
                <input class="input_text" type="text" name="Nome" placeholder="Nome da equipe">
                <label id="label_file" class="label_file" for="input_file">Escolha um Emblema</label>
                <input id="input_file" class="input_file" name="Imagem" type="file" placeholder="Emblema">
                <button class="btn_cadastrar" type="submit">Cadastrar</button>
            </form>
        </div>
    </div>
</section>

<section class="lista_jogadores">
    <div class="container_lista_jogadores">
        <h2>Lista de Times</h2>

        <table>
            <thead class="titulo_tabela">
                <th>Id</th>
                <th>Nome da Equipe</th>
                <th>Emblema</th>
                <th></th>
            </thead>

            <tbody>
                @foreach (var equipe in ViewBag.Equipes)
                {
                    <tr>
                        <td>@equipe.IdEquipe</td>
                        <td>@equipe.Nome</td>
                        <td> <img class="imagem_tabela" src="/img/equipes/@equipe.Imagem"> </td>
                        <td> <a class="link_times" asp-area="" asp-controller="Equipe" asp-action="Excluir" asp-route-id=@equipe.IdEquipe>excluir</a> </td>
                    </tr>
                }
            </tbody>

        </table>
    </div>
</section>
<script	src="~/js/site.js"></script>

@* <table class="table table-striped table-responsive">

    <thead>
        <th>ID</th>
        <th>Nome</th>
        <th>Imagem</th>
    </thead>

    <tbody>
        @foreach (var equipe in ViewBag.Equipes)
        {
            <tr>
                <td>@equipe.IdEquipe</td>
                <td>@equipe.Nome</td>
                <td> <img src="/img/equipes/@equipe.Imagem" width="50px"> </td>
                <td> <a asp-area="" asp-controller="Equipe" asp-action="Excluir" asp-route-id=@equipe.IdEquipe>excluir</a> </td>
            </tr>
        }
    </tbody>

</table> *@